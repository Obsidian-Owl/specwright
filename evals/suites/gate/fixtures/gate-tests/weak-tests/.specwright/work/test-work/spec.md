# Spec: Calculator Module

## Acceptance Criteria

### AC-01: Addition returns correct sum
add(a, b) returns the arithmetic sum of two numbers.

### AC-02: Division handles division by zero
divide(a, b) throws an error when b is 0.

### AC-03: Multiply returns correct product
multiply(a, b) returns the arithmetic product of two numbers.
