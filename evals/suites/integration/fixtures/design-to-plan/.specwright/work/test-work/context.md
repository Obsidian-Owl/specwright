# Test Feature Context

## Key Files
- `src/middleware/` — middleware directory
- `src/config.ts` — configuration module
- `package.json` — dependencies

## Patterns
- Middleware follows Express pattern: `(req, res, next) => {}`
- Config loaded from environment variables
