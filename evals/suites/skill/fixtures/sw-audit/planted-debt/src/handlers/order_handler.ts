// ISSUE 3: Inconsistent naming — this file uses snake_case, violating the constitution's camelCase rule

export function handle_order_request(orderId: string) {
  return `Processing order ${orderId}`;
}
