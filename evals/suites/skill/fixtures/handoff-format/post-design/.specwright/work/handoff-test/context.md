# Context: Hello World Function (handoff-format fixture)

The fixture has no real language toolchain. The sw-plan invocation should
produce a `spec.md` and `plan.md` describing the trivial hello-world work,
then emit the three-line gate handoff. Nothing else.
